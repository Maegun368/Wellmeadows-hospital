{
  "build": {
    "command": "composer install --optimize-autoloader --no-dev && php artisan migrate --force && php artisan db:seed --force && npm install && npm run build && php artisan optimize:clear"
  },
  "start": {
    "command": "php artisan optimize && frankenphp run --config /Caddyfile"
  }
}