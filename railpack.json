{
  "build": {
    "command": "composer install --optimize-autoloader --no-dev && npm install && npm run build && php artisan optimize:clear"
  },
  "start": {
    "command": "php artisan migrate --force && php artisan db:seed --force --verbose 2>&1 && php artisan optimize && frankenphp run --config /Caddyfile"
  }
}